export const metadata = {
  title: "로그인 | 프다!",
  description: "프다에 로그인하고 나만의 학습을 시작해 보세요.",
  robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
  return children;
}
