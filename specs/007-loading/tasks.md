# 작업 목록: 공통 전체 화면 로딩

**입력**: `/specs/007-loading/`의 설계 문서

**필수 문서**: `plan.md`, `spec.md`

**참고 문서**: `research.md`, `data-model.md`, `contracts/Loading.md`, `quickstart.md`

**테스트**: 자동 테스트 도입은 요청되지 않았다. 개발 확인 화면의 수동 검증과 lint·build를 수행한다.

**구성**: 사용자 스토리별 독립 검증과 작은 커밋 경계를 기준으로 작업을 나눈다.

## 형식: `[ID] [P?] [스토리?] 설명과 파일 경로`

- **ID**: 실행 순서에 따른 `T001`, `T002` 형식의 번호
- **[P]**: 다른 파일에서 의존성 없이 병렬로 수행할 수 있는 작업
- **[US#]**: 명세의 사용자 스토리 번호

## 1단계: 준비

**목적**: 구현 범위, 자산과 기존 프로젝트 규칙 확인

- [x] T001 [P] `AGENTS.md`, `.specify/memory/constitution.md`, `specs/007-loading/spec.md`, `specs/007-loading/plan.md`, `specs/007-loading/contracts/Loading.md`에서 파일 범위·네이밍·공개 계약을 확인한다.
- [x] T002 [P] `public/images/프로필.webp`, `src/styles/abstracts/_colors.scss`, `src/styles/abstracts/_variables.scss`, `src/styles/abstracts/_typography.scss`, `src/app/(dev)/dev/layout.js`를 확인해 사용할 자산·스타일 토큰·개발 경로 구조를 기록한다.

**확인 지점**: 새 패키지, `loading.js`, 실제 요청, 기존 개발 메인 페이지 수정 없이 승인된 세 파일만 구현할 준비가 완료됨

---

## 2단계: 공통 선행 작업

이 기능은 새 기반 구조, 외부 의존성 또는 공통 상태 계층이 필요하지 않으므로 별도의 공통 선행 구현 작업이 없다.

---

## 3단계: 사용자 스토리 1 - 요청 중 전체 화면 로딩 확인 (우선순위: P1) 🎯 MVP

**목표**: 호출 측이 Loading을 표시하면 viewport 전체를 덮고 뒤쪽 조작을 차단하며, 제거하면 조작이 복구되는 기본 기능을 제공한다.

**독립 검증**: `/dev/loading`에서 `isLoading`을 켜고 끄며 오버레이 한 개 표시, 포인터·키보드 차단과 제거 후 조작 복구를 확인한다.

### 사용자 스토리 1 구현

- [x] T003 [US1] 공개 props 없이 전체 화면 오버레이와 상태 안내 영역을 항상 렌더링하고 `role="status"`, live 안내와 처리 중 의미를 제공하는 기본 `Loading` 구조를 `src/components/Loading.jsx`에 작성한다.
- [x] T004 [US1] viewport 전체 고정 배치, 연한 회색 배경막, 충분한 stacking level, 중앙 정렬과 포인터 입력 차단의 기본 스타일을 `src/components/Loading.module.scss`에 작성한다.
- [x] T005 [US1] Boolean `isLoading` 토글, 뒤쪽 확인용 입력·버튼, 로딩 중 컨트롤 비활성화와 `aria-busy` 상태를 포함하는 개발 확인 화면을 `src/app/(dev)/dev/loading/page.js`에 작성하고 `src/app/(dev)/dev/page.js`는 수정하지 않는다.
- [x] T006 [US1] `specs/007-loading/quickstart.md`의 기본 표시·상호작용 차단 절차로 `/dev/loading`을 확인하여 포인터와 Tab·키보드 입력이 뒤쪽 컨트롤을 실행하지 않고 Loading 제거 후 조작이 복구되는지 검증한다.

**확인 지점**: 사용자 스토리 1을 독립 실행할 수 있으며 전체 화면 차단 MVP가 완료됨

**커밋 경계 1**: T003~T006 완료 후 중단하고 `Loading.jsx`, `Loading.module.scss`, 개발 확인 페이지의 변경 내용과 검증 결과를 보고한다.

---

## 4단계: 사용자 스토리 2 - 일관된 로딩 디자인 확인 (우선순위: P2)

**목표**: 첨부 디자인의 프로필 이미지, 막대형 회전 스피너와 고정 두 줄 문구를 공통 Loading에 완성한다.

**독립 검증**: Loading을 단독 표시하고 첨부 디자인과 중앙 이미지, 막대 수·배치·회전, 두 줄 문구, 배경 색상과 간격을 비교한다.

### 사용자 스토리 2 구현

- [ ] T007 [US2] `next/image`로 `/images/프로필.webp` 장식 이미지를 표시하고, 원형 배치를 위한 흰색 막대 12개와 `잠시만 기다려주세요.`, `로딩중입니다` 두 줄을 `src/components/Loading.jsx`에 추가한다.
- [ ] T008 [US2] 중앙 이미지 크기, 12개 막대의 원형 배치, 흰색 색상, 반복 회전 keyframes, 두 줄 문구의 색상·타이포그래피·간격을 첨부 디자인에 맞춰 `src/components/Loading.module.scss`에 작성한다.
- [ ] T009 [US2] `prefers-reduced-motion: reduce`에서 회전 속도를 현저히 낮추면서 이미지와 문구로 상태를 유지하는 스타일을 `src/components/Loading.module.scss`에 추가하고 기본·모션 감소 양쪽을 `/dev/loading`에서 검증한다.

**확인 지점**: 사용자 스토리 1의 차단 동작을 유지하면서 첨부 디자인의 주요 요소가 모두 표시됨

**커밋 경계 2**: T007~T009 완료 후 중단하고 디자인 비교와 모션 감소 검증 결과를 보고한다.

---

## 5단계: 사용자 스토리 3 - 로딩 제어 방식 구분 (우선순위: P3)

**목표**: 이벤트 요청과 렌더 대기의 소유권을 구분하고 같은 작업에서 전체 화면 Loading이 중복되지 않는 사용 계약을 검증한다.

**독립 검증**: 개발 확인 화면에 전체 화면 오버레이가 한 개만 렌더링되고, 구현 파일에 `loading.js`, API, Promise 판정, 이동 또는 전역 로딩 상태가 없는지 확인한다.

### 사용자 스토리 3 구현 및 검증

- [ ] T010 [US3] `src/app/(dev)/dev/loading/page.js`가 `isLoading && <Loading />` 단일 조건으로 표시를 소유하고 같은 작업에 Suspense fallback을 중복 연결하지 않도록 정리한다.
- [ ] T011 [US3] `src/components/Loading.jsx`가 API, Promise 판정, 인증, 데이터 요청, 오류 처리, 이동, 로딩 상태와 전역 중복 조정을 포함하지 않고 `specs/007-loading/contracts/Loading.md`의 props 없는 UI 계약만 수행하는지 검증한다.
- [ ] T012 [US3] 저장소에서 새 `loading.js` 파일과 승인 범위 밖 페이지 연결이 생성되지 않았으며 `/summary`, `/mypage/summaries`, `/mypage/bookmarks`, 로그인·회원가입 파일이 수정되지 않았는지 `git diff`와 파일 목록으로 검증한다.

**확인 지점**: 이벤트 작업은 `isLoading`, 실제 렌더 대기는 후속 호출부의 단일 Suspense fallback이라는 계약을 유지하며 이번 범위에는 실제 페이지 연결이 없음

**커밋 경계 3**: T010~T012 완료 후 중단하고 사용 계약 및 범위 검증 결과를 보고한다.

---

## 최종 단계: 마무리 및 공통 검증

**목적**: 전체 기능의 정적 품질과 인수 시나리오 확인

- [ ] T013 `specs/007-loading/quickstart.md`의 기본 표시, 상호작용 차단, 중복 방지와 모션 감소 시나리오를 `/dev/loading`에서 최종 실행한다.
- [ ] T014 `package.json`의 `npm run lint`를 실행하고 Loading 관련 lint 오류가 없음을 확인한다.
- [ ] T015 `package.json`의 `npm run build`를 실행하고 `/dev/loading`을 포함한 production build가 성공하는지 확인한다.
- [ ] T016 `git diff --check`와 `git status --short`로 형식 오류와 승인 범위 밖 변경이 없음을 확인하고 `specs/007-loading/tasks.md`의 완료된 체크박스를 갱신한다.

**최종 커밋 경계**: T013~T016 완료 후 전체 변경 파일, lint·build·수동 검증 결과와 권장 커밋 명령을 보고한다.

---

## 의존성과 실행 순서

### 단계 의존성

```text
준비(T001~T002)
  ↓
US1 기본 전체 화면 차단(T003~T006)
  ↓
US2 디자인·애니메이션(T007~T009)
  ↓
US3 사용 계약·범위 검증(T010~T012)
  ↓
최종 검증(T013~T016)
```

- 준비 단계 완료 후 사용자 스토리 구현을 시작한다.
- US2는 US1에서 만든 동일 컴포넌트 구조와 스타일에 디자인을 완성하므로 US1 이후 진행한다.
- US3는 완성된 컴포넌트와 개발 확인 화면의 사용 계약을 검증하므로 US2 이후 진행한다.
- 최종 검증은 모든 사용자 스토리 완료 후 실행한다.

### 사용자 스토리 독립 검증

- **US1(P1)**: 전체 화면 표시·제거와 뒤쪽 상호작용 차단만으로 독립 검증 가능한 MVP다.
- **US2(P2)**: 완성된 Loading을 단독 표시해 디자인과 애니메이션만 독립 비교할 수 있다.
- **US3(P3)**: 파일 목록과 호출 코드를 검사해 단일 소유권 및 범위 준수를 독립 확인할 수 있다.

## 병렬 실행 기회

- T001과 T002는 서로 다른 문서·자산 확인이므로 병렬 수행할 수 있다.
- 사용자 스토리 구현은 `Loading.jsx`, `Loading.module.scss`, 개발 확인 페이지를 단계적으로 공유하므로 안전한 작은 커밋을 위해 순차 수행한다.
- 자동 테스트 파일이나 별도 페이지 파일을 추가하지 않으므로 사용자 스토리 내부에 별도의 안전한 병렬 구현 작업은 없다.

## 구현 전략

### MVP 우선

1. T001~T002로 범위를 확인한다.
2. T003~T006만 완료해 전체 화면 표시·차단 MVP를 검증하고 커밋한다.
3. 이후 디자인과 애니메이션, 사용 계약 검증을 별도 커밋으로 추가한다.

### 작은 커밋 진행

1. **커밋 1**: 기본 Loading 구조, 전체 화면 차단 스타일, `isLoading` 개발 확인
2. **커밋 2**: 프로필 이미지, 막대형 스피너, 두 줄 문구와 모션 감소
3. **커밋 3**: 단일 로딩 소유권과 범위 검증 정리
4. **최종 커밋**: lint·build 및 체크리스트 완료 상태

## 참고

- 실제 로그인, 회원가입, 전체·내 요약·북마크 페이지 연결은 후속 기능 작업에서 수행한다.
- 이번 작업에서 `loading.js`, 실제 API, 인증, 데이터 요청, 라우팅, 새 패키지와 전역 로딩 상태를 추가하지 않는다.
- 각 커밋 경계에서 사용자 확인과 직접 커밋 완료를 기다린 뒤 다음 단계로 진행할 수 있다.
